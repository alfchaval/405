using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class GestorFinal : MonoBehaviour {

    //public List<GameObject> allObjects = new List<GameObject>();
    //public List<GameObject> objectsToKeep = new List<GameObject>();

    // Use this for initialization
    void Start () {
      //  allObjects.AddRange(FindObjectsOfType<GameObject>());

       
    }
	
	// Update is called once per frame
	void Update () {
		
	}

    //public void PlugIn()
    //{

    //    for (int i = 0; i < FindObjectsOfType<GameObject>().Length; i++)
    //    {
    //        if (FindObjectsOfType<GameObject>()[i].transform.parent == null)
    //        {
    //            allObjects.Add(FindObjectsOfType<GameObject>()[i]);
    //            print("Hola");
    //        }
    //    }

    //}

    //public void Flipa()
    //{
    //    foreach (GameObject a in allObjects)
    //    {
    //        foreach (GameObject b in objectsToKeep)
    //        {
    //            if (a.name == b.name)
    //                allObjects.Remove(a);
    //        }
    //    }
    //}
    //public void DropOff()
    //{
    //    foreach (GameObject a in allObjects)
    //        a.SetActive(false);
    //}
}
